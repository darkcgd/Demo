<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
	<!-- 背景图 -->
	<item android:id="@+id/background" android:drawable="@mipmap/bar_up" />
	<!-- 第二进度图 -->
	<item android:id="@+id/SecondaryProgress" android:drawable="@mipmap/bar_up" />
	<!-- 进度度 -->
	<item android:id="@+id/progress" android:drawable="@mipmap/bar_dn" />
</layer-list>